# hello-flutter 新手專案筆記

> 這是一份給「Flutter 初學者」的學習筆記，記錄這個專案做了什麼、每個檔案的用途、
> 以及怎麼把 App 跑起來。看不懂的地方先跳過，等實際遇到再回來查。

---

## 1. 這個 App 是什麼？

這是 Flutter 的**預設範例「計數器 App」**（執行 `flutter create` 後自動產生的）。

功能很簡單：畫面中間有一個數字，每按一次右下角的「+」按鈕，數字就 +1。

```
┌─────────────────────────┐
│  計數器 🎉               │  ← 上方標題列 (AppBar)
├─────────────────────────┤
│  You have pushed the    │
│  button this many times:│  ← 說明文字
│           0             │  ← 數字 (按 + 會變大)
│                    ╔══╗ │
│                    ║ +║ │  ← 右下角圓形按鈕
└────────────────────╚══╝─┘
```

雖然小，但它包含了 Flutter 最核心的觀念（見第 4 節）。

---

## 2. 專案檔案地圖

分成 3 個等級，新手只要關心「等級 1」。

### 🟢 等級 1：你會天天碰的

| 檔案 | 用途 |
|------|------|
| **`lib/main.dart`** ⭐ | 你的主程式，99% 時間都在這裡寫程式碼 |
| **`pubspec.yaml`** ⭐ | App 的設定檔 + 要安裝的套件清單 |
| `test/widget_test.dart` | 自動化測試（新手可先略過） |

- **`lib/`**：所有你寫的 Dart 程式碼都放這裡。App 變大後會在這開更多檔案。
- **`pubspec.yaml`**：管 3 件事 —— App 名稱版本、要裝的套件（dependencies）、資源檔（圖片字型）。
  改完套件要執行 `flutter pub get` 把套件抓回來。注意 `.yaml` 用**縮排分層，空格不能亂打**。

### 🟡 等級 2：設定檔（偶爾看，先別改）

| 檔案 | 用途 |
|------|------|
| `analysis_options.yaml` | 程式碼「糾錯規則」(linter)，寫不好會畫黃色波浪底線提醒 |
| `pubspec.lock` | 自動產生，記錄實際裝的套件版本。**永遠別手動改** |
| `README.md` | 專案說明書，給人看的，跟執行無關 |

### 🔵 等級 3：各平台資料夾（現在完全無視）

同一份 `lib/` 程式碼，靠這些「外殼」變成各平台的 App：

| 資料夾 | 平台 |
|--------|------|
| `android/` | 安卓手機 App（目前在跑的） |
| `ios/` | iPhone App（需要 Mac 才能編譯） |
| `web/` | 網頁版 |
| `windows/` `macos/` `linux/` | 各桌面平台 |

> **Flutter 最大賣點**：只寫一份 `lib/main.dart`，就能打包成以上所有平台的 App。
> 這些資料夾是自動產生的，新手 99% 不用碰。

### ⚫ 自動產生（別手動改）

- `build/`：編譯暫存檔（`.apk` 在裡面），可刪，下次編譯會重生
- `*.iml`、`.dart_tool/`、`.idea/`：編輯器/工具的設定與快取

---

## 3. 怎麼把 App 跑起來

### 重要觀念：iOS 在 Windows 上不能跑

iOS 模擬器內建在 Xcode，而 Xcode 只能裝在 Mac。**所以 Windows 電腦無法跑/編譯 iOS。**
學習階段用 Android 或 Chrome 即可，觀念 100% 相同，未來拿到 Mac 幾乎不用改就能編 iOS。

### 「裝置」有哪些？

| 裝置 | 說明 |
|------|------|
| Chrome / Edge | 把 App 當網頁跑，最快，免開模擬器 |
| Windows (desktop) | 當成 Windows 視窗程式跑 |
| Android 模擬器 (Pixel_7) | 在電腦上模擬一支安卓手機 |

### 跑在 Android 模擬器的完整步驟

```powershell
# 1. 看有哪些模擬器
flutter emulators

# 2. 開模擬器（會跳出手機視窗，第一次開機要等 1~2 分鐘）
flutter emulators --launch Pixel_7

# 3. 確認模擬器上線了（狀態要是 device，不是 offline）
flutter devices

# 4. 把 App 跑到模擬器上（第一次編譯久，之後很快）
flutter run -d emulator-5554
```

### 跑在 Chrome（最快，新手推薦先用）

```powershell
flutter run -d chrome
```

---

## 4. Flutter 四大核心觀念

### 觀念 1：一切都是 Widget（積木）

畫面上看得到的東西全部都是 Widget —— 文字、按鈕、排版、整個 App。
App 就是把一堆 Widget（積木）一層包一層組起來的。

程式起點：
```dart
void main() {
  runApp(const MyApp());   // 啟動 App，把第一塊積木放上去
}
```

### 觀念 2：兩種 Widget —— Stateless vs Stateful

- **StatelessWidget（無狀態）**：畫面不會自己改變的東西。例：App 外殼 `MyApp`。
- **StatefulWidget（有狀態）**：畫面會變動的東西。例：會變大的計數器頁面 `MyHomePage`。

> 口訣：**畫面會變 → 用 Stateful；不會變 → 用 Stateless。**

Stateful 會拆成兩個 class：一個放設定（Widget），一個放會變的資料（State）。
會變的資料（如 `_counter`）放在 State 裡才不會被丟掉。

### 觀念 3：setState —— 通知畫面更新

```dart
void _incrementCounter() {
  setState(() {
    _counter++;          // 改資料
  });                    // setState 通知 Flutter「請重畫畫面」
}
```

> **新手最常踩的坑**：如果只寫 `_counter++` 而沒包在 `setState()` 裡，
> 資料雖然變了，但**畫面不會更新**，數字會卡住不動。

### 觀念 4：build —— 用巢狀 Widget 排版

```dart
Scaffold(                       // 頁面骨架
  appBar: AppBar(...),          // 上方標題列
  body: Center(                 // 置中
    child: Column(              // 直向排列
      children: [
        Text('...'),            // 說明文字
        Text('$_counter'),      // 數字
      ],
    ),
  ),
  floatingActionButton: ...,    // 右下角按鈕
)
```

常用排版 Widget：

| Widget | 作用 |
|--------|------|
| `Scaffold` | 頁面骨架（標題列、按鈕、主體） |
| `Column` | 直向（上下）排列 |
| `Row` | 橫向（左右）排列 |
| `Center` | 置中 |
| `Text` | 顯示文字 |

### 「按一下按鈕」完整流程

```
按下「+」按鈕
  → 觸發 onPressed → 呼叫 _incrementCounter()
  → setState 把 _counter +1 並通知 Flutter
  → Flutter 重新執行 build()
  → 畫面上的數字重畫 → 你看到數字變大
```

---

## 5. 開發時必備：Hot Reload（熱重載）

App 用 `flutter run` 跑起來後，終端機會停在這個選單：

```
Flutter run key commands.
r Hot reload.    ← 改完程式碼存檔後，按 r 立刻更新（狀態不丟，數字不歸零）
R Hot restart.   ← 整個 App 重來（數字歸零）
q Quit.          ← 結束 App
```

> 注意：選單出現後，下面會狂跳一堆 log 文字把選單推走，這是正常的。
> 只要終端機「停住、沒回到 `$` 提示符號」，App 就還連著，按 r / q 都有效。

**Hot Reload 操作流程**：改 `lib/main.dart` → 存檔 (Ctrl+S) → 在終端機按 `r` → 看模擬器即時更新。

---

## 6. 常用指令小抄

```powershell
flutter doctor                       # 檢查開發環境有沒有裝好
flutter devices                      # 看有哪些裝置可以跑
flutter emulators                    # 看有哪些模擬器
flutter emulators --launch Pixel_7   # 開模擬器
flutter run -d emulator-5554         # 把 App 跑到指定裝置
flutter run -d chrome                # 跑在 Chrome
flutter pub get                      # 改完 pubspec.yaml 後，抓取套件
flutter clean                        # 清掉 build 暫存（遇到怪問題時用）
```

---

## 7. 疑難排解

### 模擬器卡在 offline / 開不起來

模擬器有時會卡住（記憶體很低、一直 offline）。解法是**冷開機**（強制從頭開機，跳過壞掉的存檔）：

```powershell
# 1. 先把卡住的模擬器關掉（在工作管理員結束 qemu-system-x86_64 / emulator）
# 2. 用冷開機重新啟動：
#    （emulator.exe 位置通常在 使用者資料夾\AppData\Local\Android\Sdk\emulator\）
emulator.exe -avd Pixel_7 -no-snapshot-load
```

### flutter run 出現 "Lost connection to device"

代表 App 跟模擬器的連線斷了。通常重跑一次就好：

```powershell
flutter devices                  # 先確認模擬器還在 (device 狀態)
flutter run -d emulator-5554     # 再跑一次
```

### 怎麼分辨 flutter run 成功還失敗

- ✅ 成功：終端機停住、游標閃爍、**不會回到 `$`**（App 連著）
- ❌ 失敗：出現 `Lost connection` 或錯誤，**跳回 `$`**（可以打字了）

---

## 8. 建議的下一步練習

1. **先玩壞它**：把 `lib/main.dart` 的文字、顏色、數字亂改，存檔按 `r` 看變化（改壞能還原）。
2. **改功能**：讓按鈕「每次 +2」，或加一個「歸零」按鈕。
3. **學排版**：把 `Column`（直排）改成 `Row`（橫排）看差別。
4. **多按鈕練習**：做出「+」「−」「歸零」三個按鈕的計數器（練 setState + Row 排版）。

---

_最後更新：2026-06-04_
